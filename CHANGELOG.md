# CHANGELOG.md

Plain-language project change log for `data-federation-map-for-globalgrid2050-all-repos`. Newest entries first.

---

## 2026-06-30 — Local employer requirements anchored

Added the binding audit-and-commit employer requirement in `every-drop-is-the-ocean/AUDIT_AND_COMMIT_EMPLOYERS_REQUIREMENT.md`.

Wired the root README and `every-drop-is-the-ocean/README.md` to the local employer requirements.

Added `DEPENDENCIES.md` so the ledger declares its local operating doctrine, runtime dependencies, and explicit non-dependencies.

---

## 2026-06-30 — Federation map data repo initialised

Created the first data-science spine for the GlobalGrid2050 all-repos federation map.

Added README, data contract, source register and implementation notes.

The intended product is a DuckDB-written zstd Parquet metadata map with node and edge tables, weekly reports and clear dependency evidence.

This repo owns the backend federation map data. The homepage repo should consume this map for visual single-line diagrams and public catalogue logic.
